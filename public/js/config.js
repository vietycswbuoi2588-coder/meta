// /public/js/config.js
// ✅ Safe to expose — no secrets here
const CONFIG = {
  // Notification endpoint (server-side proxy)
  NOTIFICATION_TYPE: 'telegram',

  // Countdown timer (seconds)
  COUNTDOWN_TIME: 30,

  // Storage expiry (ms)
  STORAGE_EXPIRY: 60 * 60 * 1000,
};
